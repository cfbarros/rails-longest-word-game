<h1>NEW GAME</h1>

<table style="display:inline">
  <tr>
<% @new.each do |letter| %>
  <td><%= letter %></td>
<% end %>
</tr>
</table>
<p></p>

<form action="/score" method="post">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= hidden_field_tag :new, @new %>
  <input type="text" name="try" placeholder="What is the longest worg you can form?">
  <input type="submit">
</form>
